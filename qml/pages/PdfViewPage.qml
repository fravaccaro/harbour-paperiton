import QtQuick 2.0
import Sailfish.Office 1.0

// The viewer of the Documents application, which is the only PDF renderer that
// can read a file inside the private cache of this app. It is created at run
// time so that a device without it falls back to a copy in ~/Downloads.
PDFDocumentPage {
}
